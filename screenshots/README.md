# Screenshots

| File         | Used by                                          |
|--------------|--------------------------------------------------|
| `queue.png`  | Home page hero — device mockup of the queue      |
| `og.png`     | Social preview, 1200×630                         |

`queue.png` is the framed/tilted device render from the mockup, exported with a
transparent background. It is displayed at up to 400px wide, so export at 2–3x
(800–1200px). If the file is missing the figure removes itself rather than
showing a broken image.
